// setting
// 
// @author  firede(firede@firede.us)
// @desc    default settings, you can overriding it by yourself.


// off stylus compatibility settings (ex: keyframes), autoprefixer do it.
vendors = official

// the unit using `px` value(100% == 16px)
$-base-font-size ?= 16px

// @see http://iosfonts.com
$-base-font-family ?= 'Helvetica Neue', sans-serif

// default image dppx (dots per px unit)
$-image-dppx ?= 2

// breakpoint slice
// default value: 0 400px 600px 800px 1050px
// slice number: <- 1 |  2  |  3  |  4  |  5 ->
$-breakpoint-slice ?= 0 400px 600px 800px 1050px
